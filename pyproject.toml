[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "travel-agent"
version = "0.1.0"
description = "Multi-Agent 智能旅行规划 Web 系统"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
